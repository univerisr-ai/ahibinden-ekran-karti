name: Sahibinden Bot (GitHub Actions)

on:
  workflow_dispatch: # GitHub üzerinden manuel olarak "Run workflow" butonu ile çalıştırmak için
    inputs:
      headless:
        description: 'Chrome headless calissin mi?'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      max_pages_per_segment:
        description: 'Segment basina maksimum sayfa'
        required: true
        default: '20'
  schedule:
    - cron: '0 0 * * 1' # Her pazartesi 03:00 Turkiye saati

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    
    steps:
    - name: Kodu İndir
      uses: actions/checkout@v3

    - name: Node.js Kurulumu
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Gerekli Paketleri Yükle
      run: npm install

    - name: Google Chrome'u Kur (Playwright için)
      run: |
        npx playwright install chrome
        npx playwright install-deps chrome

    - name: Sahibinden Playwright Profil Cache
      uses: actions/cache@v4
      with:
        path: .playwright/sahibinden-profile
        key: sahibinden-playwright-profile-${{ runner.os }}-v1
        restore-keys: |
          sahibinden-playwright-profile-${{ runner.os }}-

    - name: Botu Başlat (Proxy Kapalı)
      env:
        USE_WARP_PROXY: 'false'
        USE_SCRAPEDO_PROXY: 'false'
        HEADLESS: ${{ inputs.headless || 'true' }}
        MAX_PAGES_PER_SEGMENT: ${{ inputs.max_pages_per_segment || '20' }}
        SAHIBINDEN_PERSISTENT_CONTEXT: 'true'
        SAHIBINDEN_USER_DATA_DIR: .playwright/sahibinden-profile
        SAHIBINDEN_STORAGE_STATE_B64: ${{ secrets.SAHIBINDEN_STORAGE_STATE_B64 }}
      run: |
        if [ "$HEADLESS" = "true" ]; then
          node src/main.mjs
        else
          xvfb-run --auto-servernum --server-args="-screen 0 1366x768x24" node src/main.mjs
        fi

    - name: Sonuç Listesini Çıktı Olarak Al (Artifact)
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: sahibinden-ilanlar
        path: output.json
